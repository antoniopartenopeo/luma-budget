/**
 * Local runtime user context.
 * The app is currently single-user (local-first), but this avoids hardcoded
 * scattered literals and gives one seam for future auth/multi-user support.
 */
export const LOCAL_USER_ID = "user-1"
